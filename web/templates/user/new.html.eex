<h2>New user</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_path(@conn, :create),
                        behaviour: "create" %>

<%= link "Back", to: root_path(@conn, :index) %>
