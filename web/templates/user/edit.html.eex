<h2>Edit user</h2>

<%= render "form.html", changeset: @changeset,
                        action: user_path(@conn, :update),
                        behaviour: "update" %>

<%= link "Back", to: root_path(@conn, :index) %>
