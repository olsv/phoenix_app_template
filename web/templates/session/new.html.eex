<h2>Login</h2>

<%= render "form.html", changeset: @changeset,
                        action: session_path(@conn, :create) %>

<%= link "Google+", to: oauth_path(@conn, :new, "google") %>
<%= link "VK", to: oauth_path(@conn, :new, "vk") %>
